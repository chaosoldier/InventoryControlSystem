CREATE TABLE IF NOT EXISTS t_user (user_id integer GENERATED BY DEFAULT AS IDENTITY, user_birth date NOT NULL, created_at TIMESTAMP NOT NULL, user_email varchar(64) NOT NULL, user_name varchar(255) NOT NULL, user_password varchar(255) NOT NULL, updated_at TIMESTAMP NOT NULL, VERSION integer NOT NULL, PRIMARY KEY (user_id))
ALTER TABLE t_user ADD CONSTRAINT uk_user_email UNIQUE (user_email)
CREATE TABLE IF NOT EXISTS book_data (bookId integer GENERATED BY DEFAULT AS IDENTITY, bookName varchar(255),bookPublisher varchar(255),bookIsbn10 integer,bookIsbn13 integer,bookTerm integer,bookInsrtedAt TIMESTAMP, bookUpdatedAt TIMESTAMP, PRIMARY KEY (user_id)))